{
    "version": 2,
    "name":"my-server",
    "builds": [
      { "src": "server/server.ts", "use": "@vercel/node" }
    ],
    "routes": [
      { "src": "/(.*)", "dest": "server/server.ts" }
    ]
  }